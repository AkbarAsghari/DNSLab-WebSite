@using System;
@using System.Collections.Generic;
@using System.Linq;
@using System.Text.RegularExpressions;

<MudTextField T="string" @bind-Text="input" Label="تگ ها" Clearable="true"
    Class="mb-2" />
<MudIconButton OnClick="SubmitTag" Class="ml-4" Color="Color.Primary" Variant="Variant.Filled" Icon="@Icons.Material.Filled.Add" />

@if (IsContainSpecialCharacter)
{
    <MudAlert Severity="Severity.Error" ContentAlignment="HorizontalAlignment.Right">
    <MudText>امکان درج کاراکتر های خاص وجود ندارد</MudText>
</MudAlert>
}
@if (Tags != null && Tags.Any())
{
    @foreach (var tag in Tags)
    {
        <MudFab Size="Size.Small" Class="mr-1" Color="Color.Secondary" DisableElevation="true" StartIcon="@Icons.Material.Filled.Close" Label="@tag"
    OnClick="() => DeleteTag(tag)" />
    }
}
