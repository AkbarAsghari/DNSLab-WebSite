@inject IPageRepository _PageRepository
<MudText Typo="Typo.h3">آخرین مطالب سایت</MudText>

@if (lastContents != null)
    @foreach (var item in lastContents)
    {
        <MudLink Href="@item.URL">
            @item.Title
        </MudLink>
        <MudDivider />
    }