@inject IIPRepository iPRepository
@inject IStringLocalizer<Resource> localizer

@typeparam TItem

<EditForm Model="Model" Context="editCtx">
    <Page>
        <ChildContent>
            <MudText Typo="Typo.h2">@Header</MudText>
            <DataAnnotationsValidator />
            @Body
        </ChildContent>
        <Actions>
            <BaseButtonComponent Caption="@localizer["Check"]" Icon=@Icons.Material.Filled.Search OnClick=OnValidSubmit
                                 EditContext="editCtx">
            </BaseButtonComponent>
        </Actions>
    </Page>
    @if (Results != null && Results.Count > 0)
    {
        <MudList>
            @foreach (var item in Results)
            {
                <MudListItem>
                    <MudText Class="dir-ltr">
                        @((MarkupString)item)
                    </MudText>
                </MudListItem>
            }
        </MudList>
    }
</EditForm>
