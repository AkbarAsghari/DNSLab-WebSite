@page "/About"
@inject IAccountRepository accountReository
@inject IDNSRepository dnsRepository

<MudText Typo="Typo.h1" Align="Align.Center">تغییر آی‌پی همیشه خوب نیست</MudText>
<MudText Align="Align.Center">ما با تمرکز بر مهندسی و پیشرو بودن در فناوری، تلاش می کنیم خدمات قابل اعتمادی ایجاد کنیم که کاربرها رو خوشحال کند.</MudText>
<MudText Typo="Typo.h2">درباره DNSLab</MudText>
<MudText Typo="Typo.h3">DNS شما را کارآمدتر مدیریت می کنیم</MudText>
<MudText>ماموریت دی‌ان‌اس لب ایجاد راه حل های ایمن، قابل اعتماد است که کاربر ها بتوانند از آنها برای اتصال، مدیریت و تعامل با دستگاه های متصل به اینترنت خود استفاده کنند. ما در تلاش هستیم تا راه حل هایی ساده، شهودی و جذاب به مشتریان ارائه دهیم.</MudText>
<MudGrid>
    <MudItem md="3">
        <MudText Typo="Typo.h4">۱۴۰۰</MudText>
        <MudText>دی‌ان‌اس لب در سال ۱۴۰۰ از یک ایده برای حل مشکل آی‌پی داینامیک شروع شد.</MudText>
    </MudItem>
    <MudItem md="3">
        <MudText Typo="Typo.h4">@(isLoadingUsersCount? "---" : usersCount.ToString().EnglishToPersianNumbers())</MudText>
        <MudText>@(isLoadingUsersCount? "---" : usersCount.ToString().EnglishToPersianNumbers()) نفر از دی‌ان‌اس لب برای دسترسی از راه دور به رایانه های خود، اجرای سرور خانگی، نظارت بر دوربین IP و مدیریت DNS وب سایت های خود از جمله موارد دیگر استفاده می کنند.</MudText>
    </MudItem>
    <MudItem md="3">
        <MudText Typo="Typo.h4">@(isLoadingDNSCount? "---" : dNSCount.ToString().EnglishToPersianNumbers())</MudText>
        <MudText>تا کنون حدود @(isLoadingDNSCount? "---" : dNSCount.ToString().EnglishToPersianNumbers()) هاست نِیم در سایت دی‌ان‌اس لب ایجاد شده است.</MudText>
    </MudItem>
    <MudItem md="3">
        <MudText Typo="Typo.h4">@(isLoadingTodayIPChangesCount? "---" : todayIPChangesCount.ToString().EnglishToPersianNumbers())</MudText>
        <MudText>امروز حدود @(isLoadingTodayIPChangesCount? "---" : todayIPChangesCount.ToString().EnglishToPersianNumbers()) آی پی تغییر یافته است.</MudText>
    </MudItem>
</MudGrid>
<MudDivider Class="mt-2" />