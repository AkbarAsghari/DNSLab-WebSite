@inject NavigationManager _NavigationManager

<BitButton Class="mt-1" ButtonSize="BitButtonSize.Medium" Target="@Target" OnClick="GoTo">
    @Text

    @if (Icon is not null)
    {
        <text>@(" ")</text>
        <BitIcon IconName="@Icon.Value" />
    }
</BitButton>