<div class="card h-100 shadow border border-warning rounded-pill">
    <div class="card-body">
        <BitLabel Class="card-title text-center mb-0"><strong>@((FullName.Length > 20 ? (FullName.Substring(0,20)+ " ...") :  FullName).EnglishToPersianNumbers())</strong></BitLabel>
        <BitLabel Class="text-center mb-0">@(Amount.ToString("N0").EnglishToPersianNumbers())<small> تومان</small></BitLabel>
    </div>
</div>

@code {
    [Parameter] public string FullName { get; set; }
    [Parameter] public long Amount { get; set; }
}
